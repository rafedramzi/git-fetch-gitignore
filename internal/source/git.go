package repository

func FetchGitRepository() {
	// Check if the repo exists, if not throw error
	// Check cloned git repo date, if out of date yes throw error
	// return the string pointer
}

func UpdateGitRepository() {}
